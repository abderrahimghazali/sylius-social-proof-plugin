{% set colors = {
    'live_viewers': 'bg-blue-lt',
    'recent_purchases': 'bg-green-lt',
    'sales_counter': 'bg-orange-lt',
    'low_stock': 'bg-red-lt',
    'custom_message': 'bg-purple-lt',
} %}
<span class="badge {{ colors[data.value] ?? 'bg-secondary-lt' }}">{{ data.label }}</span>
